server:
  port: 5050
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
spring:
  profiles:
    active: prod
springdoc:
  api-docs:
    path: /api-docs